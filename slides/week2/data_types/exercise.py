# Declare a function and name it get_name. 
# Declare a variable your_global_name with global scope int the function, 
# Assign value inside. Access it outside the function and change it to outside, use call, print, to show the result.